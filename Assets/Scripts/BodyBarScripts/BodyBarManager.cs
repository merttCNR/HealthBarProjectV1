using UnityEngine;
using UnityEngine.UI;

public class BodyBarManager : MonoBehaviour
{
   public Image imageHead;
   public Image imageChest;
   public Image imageL_arm;
   public Image imageR_arm;
   public Image imageL_leg;
   public Image imageR_leg;
   //public PlayerManager playerManager;
  /* private void Update() {
        BodyCheck();
   }
    private void BodyCheck(){
        if(playerHealth <= 9){
                imageL_arm.color = Color.yellow;
                if(playerHealth <= 8){
                    imageL_arm.color = Color.red;
                }
            }

            if(playerHealth <= 7){
                imageL_leg.color = Color.yellow;
                if (playerHealth <= 6)
                {
                    imageL_leg.color = Color.red;
                }
            }
            if(playerHealth <= 5){
                imageL_arm.color = Color.yellow;
                if (playerHealth <= 4)
                {
                  imageL_arm.color = Color.red;
                }
            }
            if(playerHealth <= 3){
                imageR_leg.color = Color.yellow;
                if (playerHealth <= 2)
                {
                    bodyBarManager.imageR_leg.color = Color.red;
                }
            }
            if(playerHealth <= 1){
                imageHead.color = Color.yellow;
                imageChest.color = Color.yellow;
                if (playerHealth <= 0)
                {
                    imageHead.color = Color.red;
                    imageChest.color = Color.red;     
                }
            }
   }*/
}
